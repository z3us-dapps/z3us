import { routes } from './routes'

export const settingsMenuSlugs = {
	GENERAL: `/${routes.ACCOUNTS}/${routes.SETTINGS}`,
	ACCOUNTS: `/${routes.ACCOUNTS}/${routes.SETTINGS}/accounts`,
	ADDRESS_BOOK: `/${routes.ACCOUNTS}/${routes.SETTINGS}/address-book`,
}
